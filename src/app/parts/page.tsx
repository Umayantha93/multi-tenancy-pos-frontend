"use client";

import { FormEvent, useCallback, useEffect, useState } from "react";
import { Boxes, Plus, Search, X } from "lucide-react";
import { AppShell } from "@/components/app-shell";
import { buttonClass, ErrorMessage, inputClass, PageState, Panel } from "@/components/ui";
import { api, currentUser, money } from "@/lib/api";

type Part = { id: number; name: string; sku?: string; brand: string; type: string; model?: string; year?: number; price: string; cost_price: string; stock_qty: number; images?: string[] };
export default function PartsPage() {
  const [parts, setParts] = useState<Part[]>([]); const [search, setSearch] = useState(""); const [error, setError] = useState(""); const [open, setOpen] = useState(false); const [admin, setAdmin] = useState(false);
  const load = useCallback((term = search) => { api<{ data: Part[] }>(`/parts?search=${encodeURIComponent(term)}&per_page=100`).then((result) => setParts(result.data)).catch((caught) => setError(caught.message)); }, [search]);
  useEffect(() => { const frame = requestAnimationFrame(() => { setAdmin(currentUser()?.role === "business_owner"); load(""); }); return () => cancelAnimationFrame(frame); }, [load]);
  async function add(event: FormEvent<HTMLFormElement>) { event.preventDefault(); const form = event.currentTarget; const formData = new FormData(form); try { await api("/parts", { method: "POST", body: formData }); form.reset(); setOpen(false); load(""); } catch (caught) { setError(caught instanceof Error ? caught.message : "Could not save part."); } }
  return <AppShell title="Parts inventory" eyebrow={`${parts.length} catalog items`} action={admin ? <button onClick={() => setOpen(true)} className="flex h-10 items-center gap-2 bg-[#f5c842] px-3 text-sm font-semibold"><Plus size={18} /><span className="hidden sm:inline">Add part</span></button> : undefined}><div className="mb-5 flex max-w-3xl gap-2"><label className="relative flex-1"><Search className="absolute left-3 top-3 text-[#6f746e]" size={18} /><input value={search} onChange={(event) => setSearch(event.target.value)} onKeyDown={(event) => event.key === "Enter" && load()} className={`${inputClass} pl-10`} placeholder="Name, SKU, brand or model" /></label><button onClick={() => load()} className={buttonClass}>Search</button></div>{error && <div className="mb-5"><ErrorMessage message={error} /></div>}{parts.length === 0 && !error ? <PageState message="No parts in the catalog yet." /> : <div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-3">{parts.map((part) => <Panel key={part.id} className="overflow-hidden"><div className="grid aspect-[16/7] place-items-center bg-[#e8e5dc]"><Boxes size={42} className="text-[#a7aaa4]" /></div><div className="p-4"><div className="flex items-start justify-between gap-3"><div><p className="text-[10px] font-bold uppercase text-[#167c73]">{part.brand} · {part.type}</p><h2 className="mt-1 font-display text-2xl font-semibold uppercase leading-none">{part.name}</h2></div><span className={`shrink-0 px-2 py-1 text-[10px] font-bold ${part.stock_qty <= 5 ? "bg-[#b84837]/10 text-[#b84837]" : "bg-[#167c73]/10 text-[#167c73]"}`}>{part.stock_qty} in stock</span></div><p className="mt-3 text-sm text-[#6f746e]">{part.model || "Universal"} {part.year || ""}</p><p className="mt-4 font-display text-2xl font-semibold">{money(part.price)}</p></div></Panel>)}</div>}{open && <div className="fixed inset-0 z-50 grid place-items-center overflow-y-auto bg-black/55 p-4"><form onSubmit={add} className="my-8 w-full max-w-2xl bg-[#f3f0e8]"><div className="flex items-center justify-between border-b border-[#d7d3c8] p-5"><h2 className="font-display text-3xl font-semibold uppercase">Add inventory part</h2><button type="button" onClick={() => setOpen(false)} aria-label="Close"><X /></button></div><div className="grid gap-4 p-5 sm:grid-cols-2">{[["name", "Part name"], ["sku", "SKU"], ["brand", "Brand"], ["type", "Category"], ["model", "Compatible model"], ["year", "Compatible year"], ["price", "Selling price"], ["cost_price", "Cost price"], ["stock_qty", "Stock quantity"]].map(([name, label]) => <label key={name} className="text-xs font-bold uppercase">{label}<input name={name} type={["year", "price", "cost_price", "stock_qty"].includes(name) ? "number" : "text"} step={name.includes("price") ? "0.01" : undefined} required={!['sku','model','year'].includes(name)} className={`${inputClass} mt-2`} /></label>)}<label className="text-xs font-bold uppercase sm:col-span-2">Images<input name="images[]" type="file" accept="image/*" multiple className="mt-2 block w-full border border-[#c9c5b9] bg-white p-3 text-sm" /></label></div><div className="flex justify-end border-t border-[#d7d3c8] p-5"><button className={buttonClass}>Save part</button></div></form></div>}</AppShell>;
}